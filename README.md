# daraja-payment-wrapper
A reusable Python(fastapi) SDK and wrapper that simplifies integration with Safaricom’s M-Pesa platform via the Daraja API, providing clean and reusable components for STK Push, transaction status, C2B, and B2C operations.
